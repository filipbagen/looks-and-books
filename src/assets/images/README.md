# App images

Put images used by React components here (logo, staff photos, slideshow, etc.).

Import in components, e.g.:

```ts
import logoUrl from '../assets/images/logo.svg';
```

Static assets that must be at a fixed URL (e.g. for meta tags) belong in `public/` instead.
